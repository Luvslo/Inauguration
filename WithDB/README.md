# The Inaguration

## A text based PHP Game

### Frontend

To set up frontend, you must get a PHP server up. Whether it is through apache or a web server.

### Backend

The database must be connected and configured in `dbc.php`. The attached sql file contains an export of the database used.
